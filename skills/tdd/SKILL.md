---
name: tdd
description: Desarrollo guiado por tests. Usar cuando el usuario quiera construir features o corregir bugs test-first, mencione "red-green-refactor", o quiera integration tests.
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/tdd/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/tdd/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
