---
name: continue-task
description: Traspasa la tarea actual a otra sesión del mismo workspace — un prompt copiable por defecto, o un documento markdown si lo pides.
argument-hint: "¿En qué debería centrarse la próxima sesión?"
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/continue-task/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/continue-task/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
