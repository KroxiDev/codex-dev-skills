---
name: implement
description: "Implementa una pieza de trabajo basada en una spec o un conjunto de tickets."
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/implement/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/implement/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
