---
name: migrate-to-shoehorn
description: Migra archivos de test de type assertions `as` a @total-typescript/shoehorn. Usar cuando el usuario mencione shoehorn, quiera reemplazar `as` en tests, o necesite datos parciales de test.
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/migrate-to-shoehorn/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/migrate-to-shoehorn/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
