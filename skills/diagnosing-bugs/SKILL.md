---
name: diagnosing-bugs
description: Bucle de diagnóstico para bugs difíciles y regresiones de rendimiento. Usar cuando el usuario diga "diagnostica"/"debuggea esto", o reporte algo roto, que lanza errores, que falla o que va lento.
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/diagnosing-bugs/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/diagnosing-bugs/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
