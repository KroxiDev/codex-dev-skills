---
name: to-spec
description: Convierte la conversación actual en una spec y la publica en el issue tracker del proyecto — sin entrevista, solo síntesis de lo ya discutido.
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/to-spec/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/to-spec/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
