---
name: prototype
description: Construye un prototipo descartable para responder una pregunta de diseño. Usar cuando el usuario quiera comprobar si un modelo de estado o una lógica se sienten correctos, o explorar cómo debería verse una UI.
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/prototype/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/prototype/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
