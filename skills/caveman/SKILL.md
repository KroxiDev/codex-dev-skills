---
name: caveman
description: Modo de respuesta ultracomprimida — habla como un cavernícola inteligente. Niveles lite (por defecto), full y ultra.
argument-hint: "lite | full | ultra (por defecto: full)"
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/caveman/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/caveman/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
