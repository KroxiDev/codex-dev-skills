---
name: git-guardrails-codex
description: Añade guardrails de Git para sesiones de Codex mediante instrucciones y un verificador local. Usar al proteger un repositorio contra push, reset, clean, force o borrado accidental.
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/git-guardrails-codex/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/git-guardrails-codex/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
