---
name: resolving-merge-conflicts
description: "Usar cuando haya que resolver un conflicto de merge/rebase de git en progreso."
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/resolving-merge-conflicts/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/resolving-merge-conflicts/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
