---
name: teach
description: Enseña al usuario un skill o concepto nuevo, dentro de este workspace.
argument-hint: "¿Sobre qué te gustaría aprender?"
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/teach/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/teach/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
