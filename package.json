{
  "name": "engineering-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "build:claude": "node scripts/generate-claude-skills.mjs",
    "prepare": "husky",
    "test": "node --test",
    "validate:skills": "node scripts/validate-skills.mjs && node scripts/generate-claude-skills.mjs --check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.2"
  }
}
